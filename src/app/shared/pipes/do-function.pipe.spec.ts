import { DoFunctionPipe } from './do-function.pipe';

describe('DoFunctionPipe', () => {
  it('create an instance', () => {
    const pipe = new DoFunctionPipe();
    expect(pipe).toBeTruthy();
  });
});
